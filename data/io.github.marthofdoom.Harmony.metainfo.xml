<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>io.github.marthofdoom.Harmony</id>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>GPL-3.0-or-later</project_license>
  <name>Harmony</name>
  <summary>Create and sync playlists across YouTube Music and Qobuz</summary>

  <description>
    <p>
      Harmony is a desktop app for managing music playlists across YouTube Music
      and Qobuz. Search both services from one place, browse and edit playlists,
      and mirror or two-way sync a playlist from one service to the other with
      fuzzy track matching that surfaces ambiguous matches for review rather than
      guessing at them.
    </p>
    <p>Features:</p>
    <ul>
      <li>Unified search across both services</li>
      <li>Create, edit, and delete playlists on either service</li>
      <li>Cross-service sync with a reviewable plan before anything is written</li>
      <li>A remembered match cache and before-sync snapshots</li>
      <li>Import and export playlists as M3U, CSV, or JSON</li>
      <li>Discovery blending Last.fm, ListenBrainz, and native recommendations</li>
    </ul>
  </description>

  <launchable type="desktop-id">io.github.marthofdoom.Harmony.desktop</launchable>

  <categories>
    <category>AudioVideo</category>
    <category>Audio</category>
  </categories>

  <keywords>
    <keyword>music</keyword>
    <keyword>playlist</keyword>
    <keyword>sync</keyword>
    <keyword>youtube</keyword>
    <keyword>qobuz</keyword>
  </keywords>

  <content_rating type="oars-1.1"/>

  <releases>
    <release version="0.7.13" date="2026-08-31">
      <description>
        <p>Accounts page gains a "Sync accounts from another instance" button —
        pick a discovered instance (or type its host:port) and copy its
        credentials over, encrypted with the personal key, instead of using the
        API by hand. Saving the personal key now also keeps this browser
        authenticated, and the web app's cache is bumped so it refreshes.</p>
      </description>
    </release>
    <release version="0.7.12" date="2026-08-31">
      <description>
        <p>Credential copies now cross the network encrypted — the copied
        secrets are sealed with a key derived from the personal key (PBKDF2 →
        Fernet), so they stay confidential on the wire even over plain HTTP.
        Only a holder of the matching key can decrypt them.</p>
      </description>
    </release>
    <release version="0.7.11" date="2026-08-31">
      <description>
        <p>Federation: full instances copy credentials from a peer.</p>
        <ul>
          <li>A full instance (a second desktop, a headless server) with a
          matching personal key but no accounts of its own now pulls the
          streaming credentials from a peer — automatically on first boot, or on
          demand — so a fresh headless server becomes a working credential holder
          without the browser/OAuth onboarding dance. Light clients (phone, web)
          still just use the hub's credentials over the API, never copying them.
          The export is served only to a key-matching caller.</li>
        </ul>
      </description>
    </release>
    <release version="0.7.10" date="2026-08-31">
      <description>
        <p>The Android client is now a full light client — everything the web UI
        does except signing in (accounts stay on the hub):</p>
        <ul>
          <li>Library: browse playlists, open and play them, create / rename /
          delete, and add or remove tracks.</li>
          <li>Sync: pick a source and target playlist, preview the plan, apply.</li>
          <li>Output picker: play a track to a WiiM/UPnP device the hub knows,
          not just the phone.</li>
        </ul>
      </description>
    </release>
    <release version="0.7.9c" date="2026-08-31">
      <description>
        <p>Alpha fix — "play the hub's audio on my phone" now works reliably:</p>
        <ul>
          <li>Instead of the hub pushing UDP to the phone (which a phone rarely
          receives — Wi-Fi isolation, cellular, VPN NAT all block inbound UDP),
          the hub now serves its live output as an MP3 stream the phone pulls
          over HTTP and plays with buffering. Works on Wi-Fi, VPN, or cellular.</li>
        </ul>
      </description>
    </release>
    <release version="0.7.9b" date="2026-08-31">
      <description>
        <p>Alpha follow-up — federation fixes, a polished phone client, and the
        phone audio bridge:</p>
        <ul>
          <li>The Android client shows album art, has a cleaner Now Playing and
          search, and can bridge audio: play a hub track on a UPnP/DLNA device
          on the phone's own network by relaying the stream through the phone —
          so it works even when the hub is reached over a VPN.</li>
          <li>Fixes: "Play on this phone" now actually works (the RTP sender was
          mis-configured); instances advertise all their addresses (LAN + VPN)
          so roaming clients find a reachable one; expired stream URLs are
          re-resolved instead of failing a late seek; inter-instance routing
          negotiates a matching transport.</li>
        </ul>
      </description>
    </release>
    <release version="0.7.9a" date="2026-08-31">
      <description>
        <p>Alpha bundle — first release carrying every client + package
        together (desktop &amp; server Flatpak, Debian server package, Android
        APK):</p>
        <ul>
          <li>Inter-instance audio routing: send or receive full system audio
          between instances over the mesh, with a Music-default / Gaming latency
          preset. Play another machine's audio here, or send this machine's to
          it.</li>
          <li>The web client is now a mobile-friendly, installable PWA — a phone
          bottom-nav, touch controls, and offline app shell.</li>
          <li>A headless-server Debian package that runs Harmony as a systemd
          service.</li>
          <li>Fix: one unavailable track (no videoId) no longer fails a whole
          YouTube Music playlist.</li>
        </ul>
      </description>
    </release>
    <release version="0.7.0" date="2026-08-31">
      <description>
        <p>The mesh milestone. Harmony instances now find each other and share
        a single account, and there is a mobile client:</p>
        <ul>
          <li>Every instance serves an HTTP API (and a lightweight web UI),
          always on when the app runs — the engine other clients talk to.</li>
          <li>LAN discovery over mDNS: instances see one another automatically,
          and a light client (phone, another desktop) can join without its own
          logins.</li>
          <li>Personal key: set one key across your instances and only matching
          keys may share credentials — the gate for account sharing on every
          version.</li>
          <li>Zero-setup YouTube: one click detects the browser you are already
          signed in with (native or Flatpak) and connects — no OAuth dance.</li>
          <li>Minimize to the system tray; a right-click there actually quits.
          The API server keeps running in the background.</li>
          <li>A new Android client: discover an instance on your network,
          connect with your key, search, and play — streaming real hi-res audio
          from the hub.</li>
          <li>Fixes: YouTube playlists load reliably again.</li>
        </ul>
      </description>
    </release>
    <release version="0.6.1" date="2026-08-30">
      <description>
        <p>Playback and UI polish:</p>
        <ul>
          <li>The Now Playing seek bar now works reliably, with smooth
          second-by-second progress and a clear message when a track can't be
          seeked.</li>
          <li>Right-click pickers (Play on Device / Add to Playlist) open under
          the pointer and stay on-screen, including for songs inside a
          playlist.</li>
          <li>Local playback ("This computer") requests the highest-quality
          stream a track allows — Qobuz hi-res FLAC, YouTube's best audio — and
          shows the negotiated output format.</li>
          <li>Now-playing indicators light up the playlist a track is playing
          from; a playlist's track view refreshes when tracks are added to it.</li>
        </ul>
      </description>
    </release>
    <release version="0.6.0" date="2026-08-26">
      <description>
        <p>Milestone release. Harmony is now a full music hub, not just a
        playlist syncer:</p>
        <ul>
          <li>A persistent Now Playing bar with transport, seek, volume,
          shuffle/repeat, a device selector, and on-screen playing indicators.</li>
          <li>Local playback ("This computer") — decode and play tracks on this
          machine through PipeWire, alongside casting to devices.</li>
          <li>Route Audio — receive a low-latency network stream (ROC, with
          forward error correction) from another machine and play it locally.</li>
          <li>Qobuz streaming reliability fixes, including recovering the app
          secret used to sign streaming requests.</li>
          <li>Stability: a crash fix in album-art loading, log files for easier
          diagnosis, and clean shutdown of routed streams.</li>
        </ul>
      </description>
    </release>
    <release version="0.5.18" date="2026-08-26">
      <description>
        <p>Fixes a crash (segfault) that could happen while album art loaded in
        the Now Playing bar: the artwork texture was being created on a worker
        thread, which isn't safe for GTK. Also writes logs to a rotating file
        (cache/harmony/harmony.log) so problems are easier to diagnose.</p>
      </description>
    </release>
    <release version="0.5.17" date="2026-08-26">
      <description>
        <p>A Route Audio receiver no longer keeps running after you close the
        app: it's stopped on window close, and any survivor is terminated when
        the app exits — so a routed stream can't be left playing with no way to
        stop it.</p>
      </description>
    </release>
    <release version="0.5.16" date="2026-08-26">
      <description>
        <p>"This computer" (local playback) now appears in every play-to-device
        picker — the Search toolbar, Discover, and the Similar dialog — not just
        the right-click menu. Also clearer Qobuz sign-in guidance in the setup
        script, which now leads with the reliable token option.</p>
      </description>
    </release>
    <release version="0.5.15" date="2026-08-26">
      <description>
        <p>Adds an in-app local player: pick "This computer" as a playback
        target to decode and play a track (e.g. a Qobuz FLAC) on this machine
        through PipeWire, controlled from the Now Playing bar like any device.
        Uses GStreamer, already in the runtime.</p>
      </description>
    </release>
    <release version="0.5.14" date="2026-08-26">
      <description>
        <p>The currently-playing track now shows a play indicator next to it in
        search results and playlist views, updating live as playback moves
        through a queue.</p>
      </description>
    </release>
    <release version="0.5.13" date="2026-08-26">
      <description>
        <p>Adds a persistent Now Playing bar along the bottom of the window:
        album art, title/artist, play-pause, previous/next, a draggable seek
        bar, a volume slider (for devices that support it), shuffle and repeat,
        and a device selector. Playback state is now centralized app-wide, so
        the bar reflects and controls whatever is playing to a device.</p>
      </description>
    </release>
    <release version="0.5.12" date="2026-08-26">
      <description>
        <p>Route Audio (ROC) now rides through brief Wi-Fi stalls instead of
        tearing down the session. roc's watchdog dropped the session after only
        133ms of blank audio, so a momentary network hiccup caused a pop;
        widening the blank tolerance to 2s (and choppy to 4s) lets playback
        resume seamlessly, keeping latency low without the occasional ticks.</p>
      </description>
    </release>
    <release version="0.5.11" date="2026-08-26">
      <description>
        <p>Fixes the root cause of Route Audio (ROC) dropouts: the receiver was
        restarting its session dozens of times a second because the latency
        tolerance was too tight, so normal latency fluctuation kept crossing the
        bound. Widening it yields a single stable session (diagnosed from a
        verbose loopback: ~79 restarts in 15s before, 1 after).</p>
      </description>
    </release>
    <release version="0.5.10" date="2026-08-26">
      <description>
        <p>Fixes frequent audio dropouts on Route Audio (ROC): roc-recv's log no
        longer goes to an undrained pipe that could stall playback, the resampler
        is now high-quality, jitter tolerance is wider, and the default target
        latency has more headroom for Wi-Fi. Diagnostics are written to a log
        file. Raise the target latency if audio still breaks up.</p>
      </description>
    </release>
    <release version="0.5.9" date="2026-08-26">
      <description>
        <p>Route Audio now uses ROC when available (bundled in the Flatpak):
        forward error correction and an adaptive latency tuner keep network
        audio glitch-free at far lower latency than plain RTP over Wi-Fi. Falls
        back to RTP where roc-recv isn't present.</p>
      </description>
    </release>
    <release version="0.5.8" date="2026-08-26">
      <description>
        <p>New Route Audio page: receive a low-latency RTP audio stream from
        another machine on your LAN (e.g. a Steam Deck) and play it out a chosen
        local output device. Works in the Flatpak.</p>
      </description>
    </release>
    <release version="0.5.7" date="2026-08-26">
      <description>
        <p>Playlists can now export to and import from a plain, human-readable
        text list (one "Artist - Title" per line, with the track's universal
        ISRC id appended where known so it re-resolves exactly).</p>
      </description>
    </release>
    <release version="0.5.6" date="2026-08-26">
      <description>
        <p>Album/playlist queue playback now advances to the next track based on
        playback position reaching the track's duration, which is reliable across
        devices, instead of a device-specific stop signal.</p>
      </description>
    </release>
    <release version="0.5.5" date="2026-08-26">
      <description>
        <p>Right-click context menus now cover collections: play a whole album,
        artist, or playlist to a device, or add all of its tracks to a playlist,
        in one action. Tracks in a playlist get the same full menu as search.</p>
      </description>
    </release>
    <release version="0.5.4" date="2026-08-26">
      <description>
        <p>Discover page overhaul: recommendation results are now actionable
        (play to a device or add to a playlist right from the list), with live
        progress while suggestions load, a Regenerate button, clearer empty
        states, and friendlier source labels.</p>
      </description>
    </release>
    <release version="0.5.3" date="2026-08-26">
      <description>
        <p>Play-to-device now uses UPnP so the renderer's own screen can show the
        track (title, artist, album art) and report a duration, and the Devices
        page shows a live progress bar. Falls back automatically to direct
        control if a device has no UPnP support.</p>
      </description>
    </release>
    <release version="0.5.2" date="2026-08-26">
      <description>
        <p>Adds right-click context menus across tracks, artists, albums, and
        playlists, and a "Show Similar" music action that opens a recommendations
        view you can play or turn into a playlist.</p>
      </description>
    </release>
    <release version="0.5.1" date="2026-08-26">
      <description>
        <p>Adds selectable color themes (Appearance in Preferences), fixes
        YouTube tracks cutting off partway through when playing to a device, and
        makes the Devices page show the playing track reliably.</p>
      </description>
    </release>
    <release version="0.5.0" date="2026-08-26">
      <description>
        <p>Unified search and cross-service playlist sync across YouTube Music
        and Qobuz, discovery, WiiM/LinkPlay device control, and play-to-device
        (a passive LAN relay that streams a track to a speaker).</p>
      </description>
    </release>
    <release version="0.1.0" date="2026-08-25">
      <description>
        <p>Initial packaged release.</p>
      </description>
    </release>
  </releases>

  <developer id="io.github.marthofdoom">
    <name>marth</name>
  </developer>
</component>
